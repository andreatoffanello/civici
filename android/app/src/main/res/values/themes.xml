<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base — usato solo per la finestra. Il tema reale è in Compose (DoVeTheme) -->
    <!-- Tema XML minimale per app pure Compose — AppCompat richiesto da AppCompatActivity -->
    <style name="Theme.DoVe" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
